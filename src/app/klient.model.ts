export class Klient {
    constructor(public email: string, public telefon: number, public miasto: string, public data: string, public typ: string, public accepted: boolean, public pytania: string, public godzina: string ) {}
  }